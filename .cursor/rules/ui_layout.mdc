---
description: HTML 页面布局与 Chrome 规范
globs: backend/**/*.py
alwaysApply: false
---

# UI 布局规则

修改或新增 **HTML 页面**（`HTMLResponse`、`*_html.py`、`html/render.py`）前，必须先阅读 [`docs/standards/ui_layout.md`](../docs/standards/ui_layout.md)。

**核心原则**：**Chrome 统一，内容区可按页面类型差异化。** Dashboard 只压缩 KPI 以下内容区，不压缩 Chrome。

## Chrome 强制要求

| 项 | 规范 |
|----|------|
| CSS 单源 | 引用 [`ui_css.py`](../backend/app/ui_css.py)：`PAGE_CHROME_CSS`、`STANDARD_HEADER_CSS`、`BTN_CSS`、`TABLE_SCROLL_CSS` |
| `body` padding（Standard） | **`0.4rem 1rem 2rem`** |
| `body` padding（Dashboard） | **`2rem 1rem 0.75rem`**（`DASHBOARD_BODY_CSS`） |
| `body` padding（工作台） | **`0.4rem 1rem 0`**（`WORKBENCH_BODY_CSS`） |
| `.container` | **max-width 1400px** |
| 用户栏 | **`auth_html.inject_user_bar`**；`.auth-topbar` **绝对定位**右上角（`USER_BAR_CSS`） |
| 子页 | **必须有 breadcrumb**（`0.875rem`，`margin-bottom: 1.5rem`）；**首页除外** |
| `h1` | Standard **1.5rem**；Dashboard 品牌 **1.3rem** |
| 按钮 | **`.btn-primary` / `.btn-secondary`**；禁止 `.container button` 全局规则 |

## Dashboard 例外

- **仅 `GET /` 首页** 可使用 Dashboard 紧凑模式（`body.dashboard-page`）。
- **只允许压缩 KPI 以下内容区**。
- **不允许** 改动容器宽度或用户栏位置来换取单屏。

## 禁止项

- 用户栏并入 `page-header`
- 复制漂移 Chrome CSS 而不引用 `ui_css.py`
- `.container button { … }` 全局样式
- `body { overflow: hidden }`
- 表格页不引用 `TABLE_SCROLL_CSS`

## 新页 / 改页检查清单

1. 已读 `docs/standards/ui_layout.md`
2. 已引用 `PAGE_CHROME_CSS`（及 Dashboard/Workbench 覆盖）
3. `inject_user_bar` + 绝对定位 auth-topbar
4. 子页有 breadcrumb（首页无）
5. 按钮用语义 class；表格页含 `TABLE_SCROLL_CSS`

## 与数据规范的关系

字段 / Excel / API 命名见 [`data_model.mdc`](data_model.mdc) 与 `docs/canonical/`；本规则只管 **布局与 Chrome**。
