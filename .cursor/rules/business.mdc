---
description: Business domain — Asset Pool, Trust Product, underlying trust assets
alwaysApply: true
---

# Business Domain

资产证券化业务链：**资产包 → 信托产品 → 底层信托资产（监控/回款/发行）**

> 已废弃并 DROP：`projects`、`project_asset_pools`、`investors`、`investments`（原 demo，2026-06-29/30 migration）。

## Entity Overview

| Entity | 中文 | Table | Role |
|--------|------|-------|------|
| Asset Pool | 资产包 | `asset_pools` | 信托产品的分组容器（FK 壳，待后续治理） |
| Trust Product | 信托产品 | `trust_products` | 发行的融资/信托产品 |

## Relationships

```
Asset Pool (1) ──< (N) Trust Product ──< (N) trust_assets / 监控 / 回款 / 发行
```

| From | To | Cardinality | Join / FK |
|------|----|-------------|-----------|
| Asset Pool | Trust Product | **1 : N** | `trust_products.asset_pool_id` |

## Key Business Rules

- 一个 **Trust Product** 必须绑定且仅绑定一个 **Asset Pool**

## Aggregations (used in APIs / Dashboard)

| Metric | 计算方式 |
|--------|----------|
| 资产包数 | `COUNT(asset_pools)` |
| 信托产品数 | `COUNT(trust_products)` |
| 逾期/暴露/A 级 | 监控表 `trust_asset_monitor_records`（最新快照日） |

## Common Status Values

| Entity | Examples |
|--------|----------|
| Asset Pool | `pending`, `active` |
| Trust Product | `draft`, `raising` |

When adding features, preserve Asset Pool → Trust Product → 底层资产模块的链路。
